{
  "name": "neorest-node",
  "version": "0.1.0",
  "description": "Neorest server package (Node). Combines core and router-node for Node servers.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc -b",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@neorest/core": "^0.1.0",
    "@neorest/router-core": "^0.1.0",
    "@neorest/router-node": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}